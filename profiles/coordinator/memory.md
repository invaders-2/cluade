# memory.md — Coordinator 通用方法论

> 只写角色通用工作经验 / 固定流程 / 方法论，**不记录单个项目进度**（项目进度进 `wiki/projects/<项目>/`）。

- 分配任务时验收标准写成"可被证据验证"，不写"实现 XX 功能"。
- 大改动要求执行角色交证据链：before cat + after cat + grep + git diff。
- 跨次累计的东西（评分、决策、踩坑）必须 commit 进 `wiki/system/`，否则下次会话归零。
